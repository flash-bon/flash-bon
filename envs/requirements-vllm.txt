# Flash-BoN vLLM VERIFIER env: serves Qwen/Qwen2.5-VL-7B-Instruct for the pairwise tournament.
# Python 3.12.
#
#   conda create -n vllm_env python=3.12 -y && conda activate vllm_env
#   pip install -r envs/requirements-vllm.txt
#
# vllm pins its own compatible stack (observed: torch==2.9.0, transformers==4.57.3,
# xformers==0.0.33.post1). Nothing else is needed here.
vllm==0.11.2
